# nazhi-cli 全模块深度审查报告

> 审查方式：不使用子代理；以 cfece43（v1.6.3）为基线，逐模块阅读仓库源码，并对照本机 E:\newCC\life-new2026\nazhi\src\components\ 前端源码。源码只作为契约线索，最终结论优先采用可重复的 Go 测试、HTTP mock、CLI 黑盒和构建结果。
>
> 审查范围：pkg/client、pkg/types、pkg/logx、pkg/tokenparse、pkg/envelope、cmd/nazhi、internal、测试/集成/治理、Makefile 与 CI。
>
> 约束：审查阶段不对真实平台执行发布、编辑、删除或上传写操作；本地发现的可复现缺陷按 TDD 修复并回归验证。

## 结论摘要

- 当前已完成：认证/会话/HTTP/日志、公共类型、任务/写实/分页、荣誉/自评/典型案例/用户/文件、CLI/构建/治理的源码走读和可重复测试。
- 已复现缺陷：本轮未发现 P0/P1/P2 业务缺陷；分页虚低、限制分页零页和非法原始数组边界已通过 TDD 修复并锁定。
- 当前工作区验证基线：定向修复回归、竞态检测、vet、lint、集成 mock、治理测试和构建均通过；版本已更新为 1.6.4，待本轮最终门禁后打 `v1.6.4` 标签。

## 1. 认证、会话、HTTP 与日志

### 源码与前端对照

- 登录与会话链路位于 pkg/client/auth.go、pkg/client/session.go、pkg/client/request.go；业务激活实现为首页 → getMenu（/homepage?token=）→ getMenu（/home）→ getMyInfo 四步。SSO 登录页不在前端镜像内，因此登录 wire 仍以测试夹具/HAR 为证据。
- 上传/下载使用独立 clean client；业务 cookie 通过 pkg/client/cookie_sync.go 只写入业务域。脱敏集中在 pkg/logx/redact.go，CLI 错误信封由 cmd/nazhi/output.go 再做一次脱敏。

### 已执行验证

- go test -count=1 -race ./pkg/client ./pkg/types ./cmd/nazhi：通过。
- go vet ./...：通过。
- golangci-lint run ./...：通过。
- go test -count=1 -tags=integration -race -v ./test/integration/...：通过；真实账号链路因未注入环境凭据按设计跳过，HAR mock 链路通过。
- 认证/会话相关 mock 已覆盖 302 Location、200 JSON、空体、非 JSON、超大响应、验证码重试、上下文取消、cookie jar、session backoff、并发 fast path 与日志脱敏。

### 当前结论

- 本阶段未发现已由实测确认的 P0/P1 缺陷。
- sessionManager 持锁执行四步网络请求是明确的并发隔离设计，学校信息 SSO 回退已经在锁外；没有把“锁内有网络”直接误报为缺陷。
- readStdinWithTimeout 在取消或超时后关闭实际读取句柄，已通过管道阻塞读取回归测试；该入口仍属于单次 CLI 进程模型。
- newCleanClient 对自定义 RoundTripper 的上传隔离已在本轮修复：非标准 Transport 回退到无状态的 http.DefaultTransport，避免认证拦截器状态进入公共上传域；已有单元测试锁定。

## 2. 公共类型与响应解码

### 已执行验证

- go test -count=1 -race ./pkg/types：通过。
- 已覆盖 FlexBool、FlexInt、FlexFloat、IntList、PlayRoleCode、典型案例弹性整数、荣誉分数、写实混合 JSON tag 与统一响应容器。

### 当前结论

- 响应 envelope 的 msg、returnData/dataList/dataMap/pageBean 分层与代码一致。
- DecodeDataList 任一元素类型错误导致整页失败，属于当前明确契约；CLI 原始 JSON 透传不替代结构化 SDK 的类型测试。
- 类型阶段仍需补做边界输入黑盒（尤其弹性数字的非法小数/溢出）后再决定是否列为发现。

## 3. 任务、写实与分页

### 已执行验证

- go test -count=1 -race ./pkg/client：通过；已覆盖任务元数据、14 类 payload、预览/提交 parity、图片数量、内容 rune 长度、hours、分页并发、部分失败、context 取消、TotalPage/TotalNum 钳制。
- 前端对照已核实：四个 getStudentCircle type 只代表 tab，提交表单 content 上限 200，图片最多 2 张，任务备注来自元数据。

### 当前结论

- 当前未发现已由实测确认的 P0/P1/P2 业务缺陷。
- 已修复服务端同时提供互相矛盾的 totalNum 与 totalPage 时的漏页：结构化与原始 JSON 全量路径均使用 `max(totalPage, ceil(totalNum/pageSize))`，限制分页也使用相同下界并保留服务端原始 PageBean。HTTP mock 覆盖 totalPage=0/1 与 offset 跨页场景。
- 原始 JSON 分页入口已增加合法 JSON 数组校验；非法对象响应经 HTTP mock 返回 `ErrInvalidResponse`，不会静默变成空数组。
- 分页与原始 JSON 修复提交为 `84982af`；本轮文档已同步分页、上传隔离、stdin 取消读取和发布验证边界。

## 4. 荣誉、自评、典型案例、用户与文件

### 已执行验证

- 已阅读 pkg/client/honor.go、self_eval.go、typical_case.go、user_update.go、file.go、image_prep.go 及对应 pkg/types。
- 已对照前端 performance/performanceM.vue、main/mainLeft.vue、selfgaintloss.vue、classic/classiccanter.vue、user/modifyBox.vue；确认荣誉 typeName 反查、自评双层 JSON、典型案例状态/批量删除、用户密码载体、文件域隔离等主要契约。
- 文件下载/上传已有 mock 覆盖重定向、跨域、大小、空响应、半成品清理和中途失败。

### 待完成验证

- 用户更新中只读键单独出现时是否仍产生空更新请求，需要用 HTTP mock 直接确认。
- 自评与典型案例长度限制、荣誉字段空值和实际前端 wire 仍有 CLAUDE.md 已记录的平台语义挂账，不能仅凭源码升级。

## 5. CLI、构建、CI 与治理

### 已执行验证

- go build -trimpath -o bin/review-nazhi.exe ./cmd/nazhi：通过。
- CLI --help：通过，12 个顶层命令可见。
- CLI version：输出 status=success、code=200、版本 1.6.4。
- go test -count=1 -tags=docrules ./test/docrules/...：通过。
- go test -count=1 -tags=verify ./test/integration/verify_gitignore/...：通过。
- CLI 叶子命令 cobra.NoArgs、ID 正数、payload 顶层对象、三档退出码、quiet、日志 flag/env 优先级已有测试覆盖。

### 当前结论

- 本阶段尚未发现已复现的 P0/P1/P2 缺陷。
- Makefile/CI 使用 Unix shell 语法，但目标是 GitHub Ubuntu runner；Windows 本地主要使用 Go 命令和 PowerShell，不能将平台差异直接算作仓库缺陷。
- 已完成逐项 CLI 命令矩阵黑盒，覆盖缺 payload、缺正数 ID、负 ID、单独 offset、未知 flag 和 quiet 模式；均得到预期参数错误退出码。

## 6. 复现记录

> 本节按模块完成即时追加。每条发现必须包含定位、触发输入、实际输出、影响、严重性和建议；未经命令或测试复现的内容只放“静态风险/待验证”。

## 7. 验证边界

- 未使用真实账号进行登录、提交、编辑、删除或上传写操作。
- 真实平台未提供完整 SSO HAR；SSO 200 空体、完整登录请求字段和部分写接口语义仍以 CLAUDE.md 的已知挂账为准。
- 所有“通过”只表示对应本地测试/mock/构建命令通过，不代表线上服务当前可用。

## 8. 即时复核：用户更新与命令参数矩阵

### 复核结果

- 对照 user/modifyBox.vue：前端提交对象包含 studentUuid 空串、seat 等整表字段；SDK Structured 路径对全零输入做 no-op，对只读 nationalStudentNumber 明确忽略，对 birthday 做兼容映射。pkg/client/autofill_input_test.go 与 user_update_zero_input_test.go 已用 HTTP mock 验证实际请求体和缓存行为。
- 对照 classic/classiccanter.vue：前端典型案例 remark maxlength=198、content maxlength=1500；当前 SDK/CLI 未私自收紧长度，CLAUDE.md 已把线上服务行为标为待验证，避免把前端 maxlength 误当 SDK 已确认契约。
- 对照 main/mainLeft.vue 与 selfgaintloss.vue：自评普通文本和结构化 JSON 双层提交路径分别验证；结构化空对象被本地拒绝，未提交查询空容器归一为空成功。
- 对照 managementRightBottom.vue/mainMidSearch.vue：评论成功后读取 returnData 并保留空 returnData 宽容语义；四种写实列表 type 与 CLI 命令映射已在 pkg/client/submitted.go 和命令实现中保持一致。
- 典型案例批量删除实际请求体为纯 ID 数组，空数组、非正数和字符串 ID 在 CLI/SDK 边界拒绝；update 缺少正数 id 时不发请求。

### 结论

- 本模块未发现本地 mock 可复现的 P0/P1 缺陷。
- 仍需以真实 HAR 确认自评/典型案例长度的服务端限制，以及用户 updateMyInfo 是整表还是部分更新；这些不由源码静态推断升级。

## 9. 最终审查清单与发现分级

### 已复现问题

- 当前无已复现的 P0/P1/P2 业务缺陷。分页元数据虚低、限制分页 totalPage=0/1、原始 dataList 非数组、上传自定义传输器隔离和 stdin 取消读取均已由本轮 TDD 回归测试复现并修复；第一次带 -coverprofile=bin/review-coverage.out 的命令因 PowerShell 参数未引用，把 .out 误当 package，随后使用等价的正确引用形式重跑并通过，该项是审查命令调用错误而非项目缺陷。

### 静态风险与有意天花板

- `pkg/client/file.go:newCleanClient` 对自定义 RoundTripper 已做上传隔离：无法安全 Clone 的非标准 Transport 回退到 `http.DefaultTransport`，并由回归测试锁定；业务 `WithHTTPClient` 不再影响上传传输器。
- `cmd/nazhi/self_eval_submit.go:readStdinWithTimeout` 在取消或超时时关闭实际读取句柄，唤醒阻塞读取并回收后台 goroutine；单次 CLI 进程结束后 stdin 不再复用。
- 已修复分页元数据虚低导致漏页：结构化与原始 JSON 全量路径使用 `max(totalPage, ceil(totalNum/pageSize))`，限制分页覆盖 totalPage=0/1 与 offset 跨页；原始 `dataList` 非数组响应归 `ErrInvalidResponse`（commit `84982af`）。
- 原始 JSON 深度扫描拼装保留，用于不裁剪平台字段；入口已先确认每页是合法 JSON 数组，新增异常响应回归通过。

### 证据边界

- 本轮未使用真实账号凭据；未对线上执行写操作。
- 集成测试中 TestReal_FullChain 因环境未注入账号按设计跳过；HAR mock 场景通过并解析出 234 个任务。
- 没有为未验证的线上语义编造结论；CLAUDE.md 的既有挂账仍然有效。

### 验证命令记录

- go test -count=1 -race '-coverprofile=bin/review-coverage.out' ./...：通过。
- go test -count=1 -race ./pkg/client ./pkg/types ./cmd/nazhi：通过。
- go vet ./...：通过。
- golangci-lint run ./...：通过。
- go test -count=1 -tags=integration -race -v ./test/integration/...：通过；真实链路跳过，HAR mock 通过。
- go test -count=1 -tags=docrules ./test/docrules/...：通过。
- go test -count=1 -tags=verify ./test/integration/verify_gitignore/...：通过。
- go build -trimpath -o bin/review-nazhi.exe ./cmd/nazhi：通过；--help 与 version 黑盒通过。

## 10. CLI 黑盒参数矩阵（即时追加）

使用构建产物 `bin/review-nazhi.exe` 实测以下输入，均未发出业务请求：

| 场景 | 实际退出码 | 实际结果 |
|---|---:|---|
| `user update` 缺少 payload | 3 | error envelope，code=400 |
| `typical-case update --payload {}` 缺少正数 id | 3 | error envelope，code=400 |
| `circle comment --id -1` | 3 | error envelope，code=400 |
| `task public --offset 2` 单独 offset | 3 | error envelope，code=400 |
| `version --bad` 未知 flag | 3 | error envelope，code=400 |
| `--quiet circle delete --id -1` | 3 | 合并 stdout/stderr 后仅见 stdout 的 error envelope；stderr 保持空 |

该矩阵确认参数错误退出码、错误信封和 quiet 约束在真实可执行文件中生效。
