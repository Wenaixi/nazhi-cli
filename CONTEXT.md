# 纳智综合评价自动化

nazhi-cli 是对纳智综合评价系统（学校使用的写实记录与综合评价平台）的命令行自动化客户端与 Go SDK。
本文件是该项目的领域词汇表：统一那些在代码注释、前端源码与对话中反复被误解的词。

**这不是规格书，不含实现细节。** 需要行为契约与实现决策时看 `CLAUDE.md`（本机记忆库）与 `docs/README.md`（功能对照表）。

## 写实与任务

**写实**：学生针对某项任务提交的一条实践记录，含正文、图片与按活动类型专属的补充字段。
_Avoid_：实践记录、活动记录、投稿

**任务**：写实所挂靠的评价维度项，由任务名称、维度、类别三级构成。
_Avoid_：活动、项目、指标

**维度 / 类别 / 任务**：任务的三级选择结构，最细粒度才是可提交写实的任务。
_Avoid_：用「活动」指代任一层级

**任务元数据**：随任务一同返回的、描述该任务提交规则的辅助信息，含任务描述、是否必交图片、建议时长。
_Avoid_：任务详情、任务配置

**活动类型**：按任务类型决定的写实专属字段组，共十四类，覆盖活动项目、学科竞赛、体育项目、劳动能力等。
_Avoid_：写实类型、targetType（这是接口字段名，不是业务词）

**活动类型编号**：平台对十四类活动类型的数字编号，决定提交表单显示哪组输入框。
_Avoid_：直接说 type

## 状态与列表

**写实状态**：写实记录自身的状态。零为已发布且可编辑删除，一为已锁定不可编辑，二为已撤回并附撤回原因。
_Avoid_：草稿、未提交、待审核（平台没有这些状态）

**写实列表类型**：查询写实列表时的入口分类，四类分别是公示、教师写实、我发布的、被撤回的。它描述"从哪个列表看"，与单条写实自身的状态无关。
_Avoid_：把它当作写实状态的另一种说法

**我发布的写实**：写实状态为零、且由当前学生发布的记录集合。
_Avoid_：已提交写实

**已提交（任务维度）**：任务层面的提交状态，来自任务统计的自由文本状态字段。它描述"这个任务交没交"，与单条写实的发布状态是两回事。
_Avoid_：与「已发布」混用

**撤回原因**：写实被撤回时附带的说明文字，展示在记录的红色提示位。
_Avoid_：审核意见、驳回理由

## 列表分页

**总条数**：服务端声明的记录总数，与总页数共同决定翻页范围。
_Avoid_：总数（与"总页数"混读）

**总页数**：服务端声明的页数上限，超过安全阈值时钳制而不翻页。
_Avoid_：页数

## 响应结构

**业务响应**：平台接口返回的原始结构，包含业务码、消息与数据载荷。消息键为 msg，载荷可能出现在返回数据、列表或映射三种位置。
_Avoid_：响应体、返回结果

**CLI 响应**：命令行工具向调用方输出的统一结构，包含状态、编码、消息与数据。成功为二百，空操作为二百零四。
_Avoid_：与「业务响应」混用

**部分完成**：批量操作中部分成功、部分失败时 CLI 响应的第三种状态，退出码为一。
_Avoid_：警告、软失败

**编码**：数字化的结果标识。业务响应与 CLI 响应各有独立的编码体系，成功值不同。
_Avoid_：状态码（与「写实状态」混读）

**退出码**：命令行进程返回给操作系统的整数，取值零到三，含义是成功、部分完成、服务端或网络故障、参数错误。
_Avoid_：返回码、状态码

## 一处已订正的冲突

平台接口的列表查询参数与写实记录自身都存在名为 type 的字段，含义完全不同。
查询参数决定"从哪个列表看"，记录字段决定"这条记录本身是什么类型"。
本项目早期曾将两者混为一谈，导致写实状态被误读为列表类型。
参见 `pkg/types/circle.go` 中 `Type` 字段的订正注释。

## 示例对话

**开发者**：我看到一条写实的 `type=1`，所以它是「教师写实」列表里的记录？

**领域专家**：不对。`type=1` 在这里指这条记录自身的类型，跟你从哪个列表点进来没关系。
你查哪个列表，看的是查询参数；这条记录是什么，是记录自己的字段。

**开发者**：那 `status=0` 呢？是草稿吗？

**领域专家**：也不是。零是已发布、可以编辑删除。没有草稿这个状态。

**开发者**：那「已提交」出现在哪儿？

**领域专家**：那是任务层面的说法，描述这个任务交没交，跟单条写实发没发布是两码事。
