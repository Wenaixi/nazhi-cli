# 安全策略

## 支持策略

本项目只为 **最新 Release** 提供安全修复。发现漏洞后，修复会随下一个版本发布；
旧版本不单独打补丁，请始终保持升级。

当前版本以仓库内 `internal/version/version.go` 为准；变更历史见 [CHANGELOG.md](CHANGELOG.md)。

## 报告安全问题

如果发现安全漏洞，**请不要公开提交 Issue**。请通过以下方式私下报告：

- 联系仓库所有者（GitHub: @Wenaixi）
- 或发送邮件至 GitHub 账号关联邮箱

我们会在合理时间内响应并修复。

## 错误处理的精确性承诺

SDK 全部失败路径收敛为哨兵错误（`pkg/client/errors.go`），调用方用 `errors.Is`
即可精确区分：登录被拒 / 业务被拒 / 网络 / 服务端限流 / 服务端不可用 / 上下文取消可重试等场景，
不会把安全相关失败静默吞掉或误归类。

顶层 panic 统一由 recover 捕获：输出堆栈到 stderr、包装为错误返回，进程退出码为业务错误码，
不会出现 panic 导致的误导性成功输出。

## PII（个人可识别信息）守卫承诺

仓库测试与文档**绝不**出现真实姓名、学号、密码等 PII 原文。
`test/integration/har_pii_redacted_test.go` 实现了一道防线：

- 守卫表只存 PII 的 **SHA-256 hex 摘要**（单向不可逆）
- 扫描时对候选字符串计算 SHA-256，与表比对命中即报错
- 历史样本已全部替换为占位符

为什么用 hash 而不是明文：守卫若用 PII 原文做自检，守卫文件本身就会成为新的泄露源。
改为不可逆 hex 后，摘要不可能反推原文。

## 历史凭据清理说明

早期开发阶段曾有真实学号/密码进入 git 历史（测试夹具）。已用 `git-filter-repo`
彻底清除对象库并重写历史，受影响用户应立即在目标平台修改密码。

注意：filter-repo 只重写主仓库对象库——任何 worktree 或本地副本需手动同步，
否则旧对象可能通过其它渠道泄露。

本地工作文件 `CLAUDE.md` 已写入 `.gitignore` 防止误 track，并有
`test/integration/verify_gitignore/` 兜底测试验证忽略规则生效。

## 最佳实践

使用 nazhi-cli 时的安全注意事项：

1. **凭证管理** — CLI 接受密码作为命令行参数，注意 shell 历史记录。建议在交互式脚本中使用环境变量。
2. **API Token** — Token 通过登录响应传递，请勿在不安全网络上明文传输或记录到日志。
3. **验证码识别器** — SDK 纯 Go 构建，不内置模型或原生库；识别器由调用方通过 `WithCustomOCR` 注入，供应链面由注入方自行控制。
4. **密钥轮换** — 如果账号密码泄露，请立即在目标平台修改密码。
5. **升级建议** — 升级前阅读 `CHANGELOG.md` 的 BREAKING 项。
