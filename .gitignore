# ─── 构建产物 ─────────────────────────────
# Go 编译产物
bin/
/nazhi
*.exe
*.test
*.out

# 覆盖率 / profile
coverage.out
coverage.html
*.cover
*.prof
coverage
coverage_pkg

# 第三方 vendor（项目用 go module，不用 vendor）
/vendor/

# 分发包
/dist/
/release/

# ─── 临时文件 / 下载 ──────────────────────

# 测试 / 调试临时文件
captcha.jpg
test.png
test_upload.png
*.png
*.jpeg
debug-*
*.log
*.tmp
*.bak

# ─── 工具 / 状态 ──────────────────────────
# 本地 live-smoke 验证输出
artifacts/
# OMC 多智能体编排状态
.omc/
.omc.*

# Claude Code 本地工作目录
.claude/

# 本地开发笔记（含敏感信息，绝不提交）
CLAUDE.md

# ─── 编辑器 / IDE ──────────────────────────
.idea/
.vscode/
*.swp
*.swo
*~
*.orig
.history/

# ─── 系统元数据 ───────────────────────────
.DS_Store
Thumbs.db
Desktop.ini
ehthumbs.db
$RECYCLE.BIN/

# 意外创建的目录（git 展开环境变量导致的）
%userprofile%/

# 测试调试文件
test_get_school_id.go

# ─── 凭据与运行时配置 ─────────
# token 输出文件（nazhi login 测试产生）
token_output.json
# .env 含真实学号密码，绝不提交
.env
.env.local
.env.*.local

# review-tdd 违规记录（本地只读）
.review-tdd-violations.md

# filter-repo 临时产物 / review-tdd 工作文件
.git-rewrite/
.review-tdd-base
angle5-findings.json

# ─── 文档构建产物 ─────────────────────
# docsify / hugo / mdbook 缓存
.docsify/
.docs/
public/
node_modules/
package-lock.json
yarn.lock
pnpm-lock.yaml
# ─── E2E 究极大测试 token 缓存 ─────────────────────
.e2e_token
test/e2e/.e2e_token
test/e2e/.token_cache

# superpowers 计划目录（本地草稿，不入库）
.superpowers/

# ─── 前端源码参照库（有意入库，供文档对照）─────────
# reference/nazhi 是纳智前端完整镜像；上面 *.png/*.jpeg 全局规则会误伤其资源文件，此处豁免
!reference/**/*.png
!reference/**/*.jpeg
!reference/**/*.jpg
!reference/**/*.gif
